# Redis Docker image

## Description

Redis Docker image.

## Usage

Start the container:

```
make start logs
```

## Build

Build the image with `make`.

> Use `make help` to see available commands for this image.
